# see http://www.scons.org/wiki/Qt4Tool
#     https://launchpad.net/scons-qt4

env = Environment ()

SConscript ('src/SConscript', 'env', variant_dir='build-native', duplicate=0)
#SConscript ('src/SConscript', 'env', variant_dir='build-win32', duplicate=0)
